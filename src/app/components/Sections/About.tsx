import { SectionTitle } from '../Util/SectionTitle'

const AboutSection = () => {
    return (
        <div id="about" className="h-screen scroll-mt-[120px]">
            <SectionTitle content="About" />
        </div>
    )
}

export default AboutSection
